import React from "react";
import HeaderTitle from '../components/HeaderTitle';

const Header = () => {
    return (
        <React.Fragment>
            <HeaderTitle />
        </React.Fragment>
    )
}

export default Header;